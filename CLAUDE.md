# CLAUDE.md

## Rules

1. **Keep `docs/database-erd.puml` in sync with the entities.** Whenever you change
   a `*.entity.ts` file (add/remove/rename a table, column, or relationship),
   update `docs/database-erd.puml` to match — and vice versa. The diagram and the
   entities must always describe the same schema.
